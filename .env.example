# Homepage doodle “send to author”
# Requires liyuxin.work verified at https://resend.com/domains (see below).

RESEND_API_KEY=
HERO_DOODLE_TO_EMAIL=2039526375@qq.com
HERO_DOODLE_FROM_EMAIL=Portfolio <hello@liyuxin.work>

# Cloudflare secrets:
#   npx wrangler secret put RESEND_API_KEY
#   npx wrangler secret put HERO_DOODLE_TO_EMAIL
#   npx wrangler secret put HERO_DOODLE_FROM_EMAIL
#
# One-time Resend domain setup (to send to QQ / any inbox):
# 1. https://resend.com/domains → Add domain → liyuxin.work
# 2. Copy DNS records into Cloudflare → liyuxin.work → DNS
# 3. Wait until Resend shows “Verified”
